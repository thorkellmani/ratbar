# entities/chef/

`chef.gd` — a bare `CharacterBody2D` with WASD movement. Looks like a leftover
from a Godot template; not referenced by any manager or scene in the sim.
Safe to delete once confirmed unused.
