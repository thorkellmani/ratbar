# managers/location_manager/

`LocationManager` — read-only queries over the `Location` children in the
scene: `get_location_data()` (all of them) and `get_job_locations(job)`
(positions of locations matching a job type). No mutation, no state of its own.
